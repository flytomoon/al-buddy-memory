# MCP registry submissions (governance server)

Submit only once the server returns provenance, validFrom/validTo and confidence with every fact — that sentence is the listing's whole differentiator.

| Registry | How | Notes |
|---|---|---|
| mcpservers.org | Submit form; category Memory | 217 memory servers listed; the description must lead with "returns provenance and validity" |
| awesome-mcp-servers (punkpeye) | PR to the Memory section | One-line description, link to repo |
| Glama | Claim the auto-generated listing | Add the README badge |
| Smithery | Publish via their CLI | Optional |
| Claude Code plugin directory | Check whether third-party memory plugins are accepted; submit if so | No memory-layer vendor is listed as of Sept 2026 (unverified) |

One paragraph, reused everywhere: *"The first memory MCP server that tells the agent where a fact came from, since when it has been true, and what superseded it. Facts are never deleted, only invalidated. Local SQLite, no key, Apache-2.0."*
