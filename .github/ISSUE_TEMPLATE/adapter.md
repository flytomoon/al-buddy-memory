---
name: Conformance adapter for an export shape
about: Score a memory export shape the scorer does not recognise yet
title: "adapter: <shape name>"
labels: adapter
---

**The shape** (a hand-written sample, not a real export with real data):

```json
{ }
```

**Which fields carry** who asserted the fact / since when / until when / confidence / relations:

**How a fact is retired** in this system (kept with a date, overwritten, deleted, expiry only):

**Is there a published schema or importer?** (link)
